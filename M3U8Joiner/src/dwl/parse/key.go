package parse

import (
	//"dwl/crypto"
)

//func (p *ParseList) loadKey(url, iv string) (*crypto.Key, error) {
	/*sets := p.sets.CloneUrl(url)
	buf, err := utils.ReadBuf(sets)
	if err != nil {
		return nil, err
	}
	var ivbuf []byte
	if iv != "" {
		if strings.HasPrefix(strings.ToLower(iv), "0x") {
			iv = iv[2:]
		}

		ivbuf, err = hex.DecodeString(iv)
		if err != nil {
			return nil, err
		}
	}
	k := new(crypto.Key)
	k.IV = ivbuf
	k.Key = buf

	return k, err*/
	//return nil,nil
//}
